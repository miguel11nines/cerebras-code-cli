You are starting a new iteration of Ralph mode. Your plan, progress log, current task list, and git context are provided below. You do NOT have access to previous iteration history — rely on these artifacts and files on disk to understand current state.

IMPORTANT: The plan file (`.opencode/plans/ralph-plan.md`) contains the user's original request and all context. If you are unsure what to do, re-read it. The plan is your single source of truth.

Treat this as a fresh start: do not assume previous iteration errors or transient outputs still apply. Re-establish context from artifacts, git state, and source files.

Review the tasks, choose exactly one task to advance this iteration, and take action. Read files to re-establish context when needed. Update `.opencode/plans/progress.txt` with any new discoveries or changes. If all tasks are complete and verified, call `loopcomplete` with a summary.
